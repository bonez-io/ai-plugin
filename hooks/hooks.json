{
  "description": "Permission gate for bonez writes: prompts before the bonez MCP `memory` and `rules` tools run save/update/delete. Reads (recall, list, get) pass through untouched.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "__(memory|rules)$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/gate-write.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
